# Instructions

- Naming: snake_case, ASCII only.
- API base: `/api/v1/[resource]` (for future web services).
- Testing: add a test for each feature under `/tests`.
- Docs: every module needs a local README with purpose, tables, endpoints, and tests.
